package com.nhnacademy;

import javafx.scene.paint.Color;

public class PlayableBall extends MixedBall {
    public PlayableBall(Point point, double radius, Color color, Vector vector) {
        super(point, radius, color, vector);
    }
}
