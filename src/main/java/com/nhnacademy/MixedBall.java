package com.nhnacademy;

import javafx.scene.paint.Color;

public class MixedBall extends BoundedBall {
    public MixedBall(Point point, double radius, Color color, Vector vector) {
        super(point, radius, color, vector);
    }
}
