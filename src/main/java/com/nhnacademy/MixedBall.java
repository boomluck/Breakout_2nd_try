package com.nhnacademy;

import javafx.scene.paint.Color;

public class MixedBall extends BoundedBall {
    public MixedBall(Point point, double radius, Color color, Vector velocity) {
        super(point, radius, color, velocity);
    }
}
